# Paths (file.cache.path, app.base64.output-path) and the log directory are no
# longer set here. They now default to a standardized, environment-agnostic
# location and are overridden via environment variables (FILE_CACHE_PATH,
# BASE64_OUTPUT_PATH, LOG_DIR) instead of per-environment properties files.
# See docs/Configuration.md.
file.cache.retention-hours=24
file.cache.audit.rotation-enabled=true
file.cache.audit.archive-enabled=true
file.cache.audit.archive-retention-days=7

spring.application.name=convert2base64
logging.level.org.springframework.web.client.RestTemplate=INFO
app.default.auth.token=q4ynVHA2s4d3unfWY2ujrQ==
app.default.content.type=application/json
